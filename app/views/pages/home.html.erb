<main class="container">
  <h1>Backlog editor</h1>
  <p>A blazing fast backlogs editor built with Hotwire</p>

  <% if user_signed_in? %>
    <%= link_to "View backlogs", backlogs_path, class: "btn btn--dark" %>
  <% else %>
    <%= link_to "Sign in", new_user_session_path, class: "btn btn--dark" %>
  <% end %>
</main>
